---
weight: 30
---

import { OpenAPIPath } from '@alauda/doom/runtime'

# StepAction APIs

List the Hub StepActions visible from a Workspace Namespace or retrieve a
requested set of StepActions in one batch. Text search is performed by clients
over the list response; this endpoint does not accept a server-side search
expression.

<OpenAPIPath path="/api/v1alpha1/stepactions" />
