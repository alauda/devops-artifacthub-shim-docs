---
weight: 20
---

import { OpenAPIPath } from '@alauda/doom/runtime'

# Pipeline APIs

List the Hub Pipelines visible from a Workspace Namespace or retrieve a
requested set of Pipelines in one batch. Text search is performed by clients
over the list response; this endpoint does not accept a server-side search
expression.

<OpenAPIPath path="/api/v1alpha1/pipelines" />
