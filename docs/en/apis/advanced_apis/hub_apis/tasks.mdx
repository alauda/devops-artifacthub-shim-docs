---
weight: 10
---

import { OpenAPIPath } from '@alauda/doom/runtime'

# Task APIs

List the Hub Tasks visible from a Workspace Namespace or retrieve a requested
set of Tasks in one batch. Text search is performed by clients over the list
response; this endpoint does not accept a server-side search expression.

<OpenAPIPath path="/api/v1alpha1/tasks" />
